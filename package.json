{
  "name": "voicedraft-root",
  "version": "1.0.0",
  "description": "VoiceDraft AI Voice Writing Assistant MVP",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix desktop",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix desktop\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
